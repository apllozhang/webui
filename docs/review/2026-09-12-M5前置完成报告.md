# M5-0 前置三项完成报告（2026-09-12）

> 作者：接手方 AI 助手。对应 `docs/M5-任务书.md` M5-0 前置必做三项，每项独立提交 + 证据；全部完成后另做了一轮"从零复查"（fresh clone + 独立实测），并如实记录复查新发现的两个问题及修复。

## 0. 结论速览

| # | 任务 | 提交 | 验收标准达成 |
|---|---|---|---|
| 0.1 | F-NEW-1 修复 | `3df753d` | ✅ fresh CRLF 克隆 `rules:check` 不再报 DRIFT（修复前同环境复现 DRIFT×23）；CI 仍绿 |
| 0.2 | 密码外置改造 | `ff50662`+`ce88c50`+`267d694` | ✅ 仓库全文 grep 口令 0 命中；缺 secret 引导退出；带 secret 双部署跑通（8091/8095 门禁全 200） |
| 0.3 | 服务器密码轮换 | `4e1f0f6` | ✅ 新凭据部署一次成功（门禁全绿）；旧凭据已失效 |

## 1. M5-0.1 F-NEW-1（P1）

- **复现**：干净克隆（autocrlf=true，工作区实测 CRLF）跑克隆内自带 `scan-rules.mjs --check` → **23 文件 DRIFT**，与移交方报告一致。
- **修复**：`parseFile` 读入即 `replace(/\r\n/g, "\n")`（比对/写回统一 LF）；`.gitattributes`（`* text=auto` + `*.md eol=lf`）根治。
- **修复后**：同克隆拉取修复 → `rules:check` 零漂移（exit=0）；主仓库零漂移；CI 绿（台账 `3f109ec` 起）。
- **教训入账**：lessons.md F16 + HANDOVER §8（文本门禁读文件先做 EOL 规范化；`.gitattributes` 锁 LF）。

## 2. M5-0.2 密码外置改造

- `deploy.js` / `deploy-kit.js`：启动 `loadSecret()` 读仓库根 `deploy.secret.json`（不入库），缺失/缺字段 → 引导提示 + 退出；**两份脚本与仓库内所有文件 grep 口令 0 命中**（含清除 HANDOVER §4.1 的明文残留）；`deploy/kit/` 旧副本（与正本逐字节一致的重复）删除。
- 模板：`deploy.secret.example.json`（含 `_说明` 字段与用法）。
- **附带修复（E2E 验证暴露）**：
  1. ssh2 解析指向不存在的 `_deploy/node_modules` → 改为 `kit/tools/node_modules`（ssh2 已安装随库）；
  2. Windows GNU tar 把 `-f "D:\..."` 当远程主机 → 指定系统 bsdtar（`TAR_BIN` 可覆盖）；
  3. **F17 回归披露**：M3 生成链的 preset fontFamily 生成即非法 JS，M4-6 的 sync-shared 用生成版覆盖手写版后 skeleton-react 构建必挂（本轮 E2E 需要真实构建才暴露——"build 绿≠运行对"的镜像）。修复：fontFamily 以 `JSON.stringify` 字符串形式生成（泛型名非引号，CSS 语义正确）；F17 入 lessons + §8。
- **E2E 证据**：spec-site → 8091（资产门禁 200×5）、kit → 8095（资产门禁 200×7），均 `DEPLOY OK`。

## 3. M5-0.3 服务器密码轮换

- 方式：SSH pty 交互 `passwd`；轮换后**双向实测**：新凭据 SSH 登录成功（whoami=alec，远端容器列表可见）、旧凭据被拒绝。
- 凭据同步：`deploy.secret.json` + `D:\AIWork\10.20.30.203.txt`（Z 盘镜像在轮换时离线，待重连后由负责人处理）。
- **验收**：新凭据下真实部署一次 → 8091 五项门禁全 200（`DEPLOY OK`）。
- 注意事项：Z 盘/VPN 如复用 alec 旧凭据，下次连接需重输新密码（见 secret/txt）。

## 4. 从零复查（完成后加做的一轮）

fresh clone + 独立实测，**新发现并当场修复 1 个问题**：

| 发现 | 修复 |
|---|---|
| 首轮强制随库只提交了 ssh2 本体，缺运行时依赖 `asn1` → fresh clone 无法加载 ssh2（0.2 验收的"fresh clone 可用"在严格口径下不成立） | 以 tracked-vs-disk 差集强制补齐全部依赖（提交 `267d694`，4263 文件随库，仓库 pack 78MB）；复测：fresh clone ssh2 加载 ✓ + 新凭据登录 ✓ + `rules:check` 零漂移 ✓ + grep 口令 0 ✓ |

其余复查项：`.gitattributes`/F16 在位 ✓；缺 secret 报错行为 ✓；主仓库与 origin 同步 ✓。

## 5. 下一步

M5 三项目试点开工条件已齐备（三仓库 SSH 可读已验证：TSSKB / ale-dan-cpl-system / nvci-lite）。按任务书在各自仓库 `v6-pilot` 分支执行，页选采用建议方案，每项目推一次证据。
