# 安全状态变更通知：服务器密码已恢复旧值（2026-09-13）

> 发出：移交方。**接手方必读**——你本机的 `deploy.secret.json` 当前存的是**已作废的轮换密码**，需立即改回。

## 变更内容

负责人决定：**测试环境恢复使用原密码**（`alec` 账号，即轮换前的旧值）。当前服务器以旧密码认证。

## 接手方需要做的（立即）

1. 把你环境里的 `deploy.secret.json` 的 `password` 字段改回**旧密码**（轮换前那个；负责人已在本机同步）。
2. 不要把密码写进任何入库文件（含 issue/报告/注释）——`deploy.secret.json` 已在 .gitignore，密码只走本机文件或口头渠道。

## 当前部署状态（如实）

- 移交方本机：部署脚本已切换到**读 secret 外置架构**，`deploy.secret.json` 已写入恢复后密码；
- SSH 握手此刻对本机**持续超时**（TCP 22 可连，协议握手 20s 内不完成——疑似 sshd 对此前失败认证限流，或恢复操作后服务未就绪）。**部署暂停，待 SSH 恢复后重试**；两站 Web（8091/8095/8096）当前均 200 正常。

## Git 历史口令残留（如实告知）

旧密码存在于 4 个历史提交中（7f1a835 / 67e41a0 / ff50662 / 377bac7，含 HANDOVER 与本测试脚本的历史版本）。决策：**私有仓库 + 测试环境，接受残留**，不做 history rewrite（会破坏接手方克隆与 tag）。约束不变：任何新提交不得再引入口令。

## 顺带：本轮移交方自查修正

移交方自己的轮换测试脚本 `_pwtest.mjs` 曾内嵌口令（377bac7）——已改为读 `deploy.secret.json` 并随本通知提交。教训：测试脚本与生产脚本同等对待，口令只走 secret 文件。


## 更新（2026-09-13 晚）：SSH 握手仍超时

移交方三次部署重试均止步于 `Timed out while waiting for handshake`（TCP 22 可连、协议握手 20s 不完成）；同期 8091/8095/8096 Web 端口全部 200 正常。判定为 **服务器 sshd 侧问题**（疑似：轮换操作/多次失败认证触发限流，或恢复密码时 sshd 配置变更未生效完），**不是密码错误**（密码错误会返回 AUTH-FAIL 而非握手超时）。

**请负责人或接手方在服务器上检查**：`sudo sshd -T | grep -E 'maxauth|maxstart|percents'`、`/var/log/auth.log` 尾部；必要时 `sudo systemctl restart sshd`。恢复后移交方部署即可跑通（本机 secret 与脚本已就绪）。
