# M5 首轮试点验收结论(移交方 → 接手方,2026-09-14)

> 复核对象:M5 启动批(`49ede0b` R20 / `22c07cb`+`3dc12d4` R19 / `d2b6a38` M5 报告;复核在 `30d4282`,其后仅 CI 台账)。
> 复核方式:全部经 GitHub 独立核验——主库代码与门禁本机复跑;三项目仓库 `v6-pilot` 分支逐一 `ls-remote` 确认、浅克隆核对;其中两个试点从零独立复现(克隆 → 装依赖 → 构建/起服 → 跑验证脚本)。

## 一、总体裁定

**M5 首轮试点验收通过。** R20、R19 达成 DoD,三仓库试点真实、变更范围与"业务逻辑零改动"声明一致,验证脚本可独立复现。M5 首轮关闭;试点发现的 4 项规范缺口回流清单成立,纳入 M6 议程。

## 二、逐项核验

| 项 | 移交方独立证据 |
|---|---|
| R20 | Controls.tsx Checkbox `invalid` 通道 + FormsDemo 传入 + IX-AGREE 断言扩展;对 218 线上复跑 check-kit:37 项全绿,`agreeAriaInvalidOnError:"true"` 实测在列 ✅ |
| R19 | design-exceptions.yml 6 条(4 项 EXEMPT + 2 项试点衍生,owner/review-by/规则 ID 齐全);`npm run exceptions:check` 本机复跑通过(60 条注册表,exit 0);CI workflow 已接台账校验步骤 ✅ |
| 试点分支 | `ls-remote` 确认三分支存在且 SHA 与报告逐一吻合:TSSKB `af91e9e`、dan-cpl `3b74791`、nvci `4680dca` ✅ |
| 变更范围 | 三仓库 diff vs main 全部为样式/令牌/桥接层/docs:dan-cpl 唯一 tsx 变更(6 行)经逐行核读为纯 className(flex-wrap/max-w-full/gap-2);TSSKB/nvci 零代码文件变更 ✅ |
| TSSKB 复现 | 浅克隆 → pip install -e . → build_site.py(661 页)→ 本地起服 → verify-tsskb.mjs:**12/12 PASS**(阅读宽度实测 760px=--reading-max、#6b489d 生效、层级无跳级、320 零溢出、资产/控制台干净)✅ |
| nvci 复现 | 浅克隆 → npm install → node server.js → verify-nvci.mjs:**8/8 PASS**(品牌令牌双变量实测 #6b489d、向导 1→2 流转、点步骤条返回后勾选与计数保持、320 零溢出)✅ |
| dan-cpl | 结构级核验(vendored tokens.css 含 #6b489d、bridge.css、验证脚本在库、截图×9 提交);未做端到端复现(vite 构建+mock 成本高,同模式已由另两试点证实)⚠️ 如后续需要可补 |
| 试点脚本 | kit/tools/_pilot/verify-{tsskb,dancpl,nvci}.mjs 三件在库,断言为真实交互(非仅渲染)✅ |

## 三、M6 议程(合并试点回流清单)

1. 版本真源一次性切换(5.4.1 → 6.0.0-rc,含全部"M2 迁移/进行中"标记清零);
2. 字体 unicode-range 分片与首屏传输预算门禁;
3. 回流项:向导/两栏工作区 320 堆叠条款(10-patterns)、状态徽章色板令牌化(20-components/badge.md,完成后删 EXC-2026-0005)、中性分区底色角色化(完成后删 EXC-2026-0006)、bridge 迁移模式入 30-governance/engineering.md;
4. 三方签收 → v6.0 Release。

## 四、复核过程纪律备注

- 移交方复核用的临时克隆、本地服务、验证产物均已清理;仓库工作树已恢复至 `30d4282` 干净状态(vendored node_modules 经一次 npm install 扰动后已 checkout 还原,未入库)。
- dan-cpl 端到端复现为唯一未覆盖项,风险低(模式同源、断言脚本在库),如评估方第三轮后要求可补跑。
