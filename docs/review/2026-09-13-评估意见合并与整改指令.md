# 评估意见合并与整改指令（移交方，2026-09-13）

> 输入：①独立评估方《ALE-WebUI-3312bf0-独立评估报告》（68/100，BLOCKED）；②移交方对其中关键声明的独立复验（全部属实）。
> 效力：本文件是给接手方的**正式整改指令**，按此执行；完成后以新固定 SHA 重新申请评估。

## 一、移交方对评估报告的裁决

**全盘接受评估结论**：`3312bf0` 不可标记/发布为 V6.0；BLOCKED 状态成立；68/100 评分合理。

**移交方复验记录**（独立于评估方机器，经 10.10.10.218 同源站点）：
- `/react/` HTML 200，但引用 `/assets/index-*.js|css` → **404**（P0-1 属实，整页空白确认）；
- `/static/` title 为 ` | ALE 知识库`、`base.html` 页脚 Logo 为绝对路径（P1-2 属实）；
- 固定提交 `vite.config.ts` 无 `base` 设置、`package.json` 无 `rules:check`（P0-2/P1 佐证属实）。

**根因归属（移交方自认）**：P0-1 的陷阱由移交方埋下——多次用临时参数 `--base=./` 修表象，**始终未把 base 固化进 vite.config.ts / package.json**，交接文档还写着 `npm run build`，接手方照文档构建即触发。此为"交付了修复、没有交付机制"的典型案例，评估方抓得对。

**同时正式撤回移交方此前的两条过度声明**：
1. HANDOVER 中"两站四入口全部健康"——经评估反证为过期状态，不是当前事实；
2. M4 报告"回归全绿"——对 8091 成立，对 Kit 四入口的端到端不成立。

## 二、整改指令（按评估方 §5 顺序，接手方执行）

| 序 | 工作包 | 完成定义（验收即按此） |
|---:|---|---|
| 1 | **React 子路径根治** | `vite.config.ts` 固化 `base: "./"`（根修，不再依赖 CLI 参数）；重建部署后 `/react/` 在 320/1440 渲染完整应用、全部资产 200、控制台零错、表格/表单/反馈链路各回归一次 |
| 2 | **门禁扩到 8095 四入口** | design:check（或新 check-kit）遍历 Hub + /react/ + /alpine/ + /static/：断言非空渲染（如 #root innerHTML 或 body 文本）、全部子资源 200、title 非空、320 根级无溢出；任一失败即门禁红 |
| 3 | **Static 修复** | base.html 页脚 Logo 改相对路径；content.json home 补顶层 `title`；重建部署后 title 非空、页脚图 200 |
| 4 | **Alpine 320 表格** | 表格容器内滚动或 min-width 方案，禁止逐字竖排；320 断言加入门禁 |
| 5 | **生成链跨平台** | `.gitattributes` 对生成物（shared/css/tokens.css、preset、spec-site css）强制 LF；`build-tokens.mjs --check` 读写双侧 EOL 规范化；`rules:check`/`rules:build` 正式注册进 package.json |
| 6 | **WARN 与治理收口** | 排查 3312bf0 上 success-bg WARN 仍出现的根因并清零；创建合法 `design-exceptions.yml`（可先空台账）+ Schema 校验进门禁；版本真源统一（5.4.1 → 6.0.0-rc 过渡决策） |

## 三、并行不变项

- M5 三项目试点在上述整改期间**暂缓**（避免在坏基线上试点）；整改完成（1–3 全绿）即恢复；
- 接手方此前停掉 Z: 盘依赖、把培训评审页重分类为临时任务等处置，移交方均**追认**。

## 四、复验方式

接手方声明完成后：以**新固定 SHA** 通知移交方与评估方；移交方按评估方 §5 复验门槛独立复跑（四入口 × 双断点 + 网络清单为空 + 门禁绿），必要时请评估方仲裁。全绿后进入 M5 试点与 M6 发布流程。
