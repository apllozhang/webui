# R11–R18 验收结论(移交方 → 接手方,2026-09-14)

> 复核对象:`035aa7d`(申请锚定;复核实际在 `3e2822d`,其后仅 CI 台账,已核实)。
> 复核方式:移交方本机独立实测(对 10.10.10.218 线上跑门禁 + DOM 量测),不采信报告转述。本轮评审会话图像通道不可用,R11 以 DOM 客观量测替代目视(更精确,方法见 §二)。

## 一、总体裁定

**R11–R18 全部验收通过。M4 正式关闭,M5 三项目试点放行启动**(TSSKB / dan-cpl / nvci,各仓库 `v6-pilot` 隔离分支;R19 台账随 M5 启动提交落地,见指令 §一 R19)。V6.0 正式发布维持冻结至 M6 收口。

## 二、逐项核验(移交方独立证据)

| # | 条目 | 移交方实测 |
|---|---|---|
| R11 | filler 空白列 | DOM 量测 1440:表宽 1150 = Σ列宽(44+140+**636**+110+110+110),余量归标题列;末列"大小(KB)"右缘 1415 = 表右缘 1415,**空白列消除**;filler 仅存在于 userSized 精确模式(源码 `{userSized && <th className="filler">}`),F14 双目标 PASS 未回归 ✅ |
| R12 | Alpine 双切换 | index.html x-init 零残留;三份 theme.js(含 shared)均有 `_inited` 幂等;线上 ALPINE/STATIC-IX-THEME 单击往返实测 `click1{dark:true,stored:"dark"}→click2{dark:false,stored:"light"}` ✅ |
| R13 | 必选同意 | 线上 REACT-IX-AGREE:未勾选提交 → 错误显示、无成功 Toast;勾选后创建成功 ✅ |
| R14 | ARIA 注入 | 线上实测 #demo-name:`aria-invalid=true`、`aria-required=true`、`required=true`、`aria-describedby="demo-name-help demo-name-error"` 且两 ID 均存在;Field 经 cloneElement 自动注入,fieldAria 死代码已删 ✅ |
| R15 | 抽屉焦点 | 线上 REACT-IX-DRAWER:打开后 focusInPanel=true、Tab 仍圈闭、关闭后 focusReturned=true ✅ |
| R16 | 面包屑 | 线上 STATIC-IX-CRUMB:目的地 = `/static/index.html`,不跳出工程档 ✅ |
| R17 | 对比度三分类 | 本机复跑输出 `PASS 42 / EXEMPT 4(出处随行) / SKIP 0 / FAIL 0`,SKIP>0 即 FAIL 已编码;dark on-action/on-action-hover 补值(neutral-0,6.91:1 / 5.15:1);tokens:check 零差异 ✅ |
| R18 | 交互门禁 | check-kit 37 项(12+20+5)对 218 全绿;断言为真实点击非桩(源码核读:ixTheme 双击往返断言 class+localStorage;ixAgreeSubmit 完整失败/成功路径);注入自测记录在案(还原双绑定 → ALPINE-IX-THEME 红;还原 href="/" → STATIC-IX-CRUMB 红),并附"仅还原 x-init 时幂等防线独立有效"的二层防线验证 ✅ |

统一命令(对 218):tokens:check 零差异 / contrast 三分类 SKIP=0 / rules:check 60 条零漂移 / design:check **37/37** / verify-colresize 双目标 PASS。CI 台账最新 success(run 34789328989 @ 9ffa7da)。

## 三、说明与备注

1. 本轮移交方与评审会话的图像读取通道均不可用,R11 未走目视验收,改用 DOM 量测(列宽求和、末列右缘对齐、空表头定位)——比截图目视更精确;唯一的无文字表头为首列复选框列(44px,合法),尾部无 filler。
2. 接手方主动披露并修复了移交方 70f9e69 时期遗漏的 shared→骨架同步(pg-right wrap,`471e2fe`),处理得当。
3. R19(design-exceptions.yml 空数组起步 + CI 校验 + 4 项 EXEMPT 首批登记)按指令随 M5 启动提交落地,届时一并复核。

## 四、下一步(M5 试点口径)

- 试点分支:三个目标仓库各建 `v6-pilot` 分支,1–2 页真实页面,引用本 kit 构建;
- 启动提交必须含 R19;试点期间发现的规范缺口按既有 review 流程回流;
- 试点页完成后再启动 M6(版本真源一次性切换、字体分片与预算、三方签收 → v6.0 Release)。
