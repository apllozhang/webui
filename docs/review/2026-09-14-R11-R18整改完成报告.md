# R11–R18 整改完成报告（接手方 → 移交方，2026-09-14）

> 对象：`docs/M5.1-整改指令-R11-R19.md`（70f9e69）R11–R18 全部必改项；R19 按指令随 M5 启动同步落地。
> 执行：接手方 AI。锚定第二轮评估提交 `3ad63fd`，每条独立提交，提交信息以编号开头。

## 一、逐条完成

| # | 条目 | 提交 | 完成证据 |
|---|---|---|---|
| R12 | Alpine 主题双切换 | `cc1b783` | 根因按指令 §〇 插桩结论：Alpine init() 命名约定自动调用 + 模板显式 `x-init="init()"` 双重调用 demoApp().init()（评估报告"表单区重复调用"归因不成立）。修复：移除 main/forms 两处冗余 x-init + shared theme.js 增 `_inited` 幂等标记 + Hub/Static 排查无同型。**线上实测：单击 dark→(localStorage=dark, .dark ✓)，再击回 light**；R18 门禁含此断言 |
| R13 | 必选同意纳入提交校验 | `64e2d7a` | agree 纳入全量校验：未勾选提交 → ErrorSummary 增"同意条款"条目（自带聚焦）+ Checkbox 下 role=alert 错误行，**无成功 Toast**；勾选后正常创建。R18 门禁断言（REACT-IX-AGREE PASS） |
| R14 | 表单错误 ARIA 关联 | `64e2d7a` | Field 经 cloneElement 自动注入 child 控件：`aria-invalid` / `aria-describedby`（=help+error ID）/ `aria-required` / `required`——调用端零记忆负担；移除零调用的 fieldAria 死代码。tsc --noEmit 通过 |
| R15 | AppShell 抽屉焦点圈闭/归还 | `213c0cd` | 复用 Drawer FB-FOCUS-001 模式：打开聚焦首项、Tab/Shift+Tab 圈闭、Esc 关闭、焦点归还触发按钮；点导航链接同步收起。R18 门禁断言（REACT-IX-DRAWER PASS） |
| R16 | Static 面包屑 + 子路径回归 | `d1e6444` | article.html `href="/"` → `index.html`；全模板+build.py 绝对引用排查零残留。R18 门禁断言目的地（STATIC-IX-CRUMB PASS：dest=…/static/index.html） |
| R11 | React 1440 filler 空白列 | `f9e7ec4` | 未进入精确模式时经 ResizeObserver 把容器余量分配给标题列（下限 240px，窄屏不塌缩）；filler 列仅在精确模式渲染。**线上实测 1440：列宽 [44,140,636,110,110,110]，filler=null**（原 396px/34.4%→0）。verify-colresize 双目标 PASS（精确模式语义不回归） |
| R17 | 对比度三分类 + 补缺值 | `e730d2d` | ① semantic.dark 补 color-on-action/on-action-hover（= neutral-0，规范 138 行角色表+附录 G 明文）：dark/on-action×action **6.91:1**（与附录 G 白字/主紫一致）、hover **5.15:1**，均 ≥4.5 PASS；② 输出改三分类：`PASS 42 / EXEMPT 4（出处见各行，规范 15 章附录 G） / SKIP 0 / FAIL 0`，**SKIP 非零即 FAIL**；③ EXEMPT 4 项随行标注规则出处；④ 全仓"46 对 ALL PASS"表述修正（HANDOVER/AGENTS 无残留、M5-任务书、workflow 注释、5 份 review 报告——历史记录以 R17 修正注标注实际分类，不删原文） |
| R18 | 交互门禁（防复发） | `6196485` | check-kit.mjs 新增 5 项交互断言（12+20+5=37）：主题单击切换×2 入口（N1）、必选同意契约（N2）、抽屉焦点圈闭/归还（N4）、面包屑目的地（N5）；Hub/React 无主题按钮记 NA。**注入回归自测见 §三** |

## 二、统一验证（§三 命令原样，目标 10.20.30.203）

```text
npm run tokens:check        # --check：零差异
npm run tokens:contrast     # PASS 42 / EXEMPT 4（规范 15 章附录 G） / SKIP 0 / FAIL 0
npm run rules:check         # 60 条；零漂移
npm run design:check        # 37/37 PASS（规范站 12 + Kit 四入口 25 含 R18 交互断言）
node verify-colresize.mjs   # F14 FIX: VERIFIED ✅（react + spec-site 双目标）
```

线上两站均已重部署（8091 规范站 tokens.css 增 dark on-action 变量；8095 含 R11–R16 全部修复）。CI：R18 后首轮 run 34789112926 **success**（runner 现场构建 + 装配 + 37 断言）。

## 三、R18 注入回归自测记录（DoD 要求）

方法：本地装配 Kit 根目录（http.server :8766）注入两类回归后跑 check-kit：

1. **还原 Alpine 双绑定完整组合**（`x-init="init()"` + theme.js 去幂等）→
   `FAIL ALPINE-IX-THEME {"click1":{"dark":false,"stored":"light"},"click2":{"dark":false,"stored":"light"}}`
   ——单击切两次回原点，正是 N1 症状；check-kit 整体红（exit 1）→ design:check 变红。
   附带发现：**只**还原 x-init（幂等保护仍在）时门禁仍绿——证明 R12 第二层防线独立有效。
2. **还原 Static 面包屑 `href="/"`** →
   `FAIL STATIC-IX-CRUMB {"dest":"http://127.0.0.1:8766/"}`（跳出工程档到站点根）。

测试后本地注入副本已弃用（未入库），门禁对干净源恢复全绿。

## 四、口径与诚实性说明

- R12 根因以指令 §〇 插桩结论为准，未采信评估报告"表单区重复调用"表述（已在提交信息中注明）。
- 本报告及此前的历史文档不再出现"46 对 ALL PASS"类总述；对比度一律以三分类计数表述。
- R18 门禁语义：交互断言仅覆盖本轮 N1/N2/N4/N5 回归面 + 必选同意成功路径；更广的交互面（排序/搜索/分页已有 verify-colresize 与人工覆盖）按 M5 试点逐步扩充。

## 五、R19（随 M5 启动同步落地，本报告不重复声明完成）

design-exceptions.yml 台账（允许空数组起步）+ CI schema/owner/review-by 校验 + R17 的 4 项 EXEMPT 作为首批记录（含规则 ID 与 owner）——在 M5 试点启动提交中一并落地。

## 六、复核申请

- **新锚定 SHA：`035aa7d`**（R18 后证据刷新提交；其前最后一个代码提交为 `6196485`，其后仅 docs/报告类提交）。
- 请按移交方惯例复核：只认你方命令输出与线上实测（§三同款命令；两站已重部署）。
- 复核通过即启动 **M5 三项目试点**（TSSKB / dan-cpl / nvci，各仓库 v6-pilot 分支），R19 随启动提交落地。
