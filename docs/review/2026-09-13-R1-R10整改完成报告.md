# R1–R10 整改完成报告（接手方 → 移交方，2026-09-13）

> 对象：`docs/M5-任务书.md`（e6fa167 合并整改指令）R1–R10 全部条目。
> 执行：接手方 AI。每条目独立提交（提交信息以编号开头），全程可审计。
> **申请以新固定 SHA 复核**：见文末。

## 一、结论

**R1–R10 全部完成**，统一验证全绿（32/32 门禁 + 4 项命令级全过），Kit 站（8095）已重部署并线上复核。CI 在整改过程中已恢复绿（见 §三 根因）。

| # | 条目 | 提交 | 完成证据 |
|---|---|---|---|
| 前置 | CI 浏览器探测回归修复 | `66d937e` | check.mjs 恢复 CHROME_PATH/PUPPETEER_EXECUTABLE_PATH + Linux 候选；CI 当轮转绿 |
| 前置 | F17 复发 + success-bg 漂移修复 | `41e0957` | preset 恢复合法 JS（node import 实测）；primitives 对齐语义层 #dff4f2，WARN 清零 |
| R1 | vite base 固化 | `b1e6a88` | dist/index.html 产出 `./assets/*` 相对引用；**线上 /react/ 资产 200、渲染完整** |
| R3 | Static 页脚 Logo 相对路径 | `8d7370c` | 全模板搜索绝对引用零命中；**线上 /static/assets/ale-logo.png 200**（同根因顺带修 NAV 绝对 href） |
| R4 | Static 首页 title | `84e930f` | content.json home 补 `"title": "首页"`；**线上 title = "首页 \| ALE 知识库"** |
| R2+R9 | 表格 320 逐字竖排 | `74c4b8b` | shared components.css `table.data` 增 min-width:640px + sync-shared 至三骨架（两条目同根因，合并提交） |
| R5 | rules:check 注册 | `02c40d0` | `npm run rules:check` 退出码 0；rules:build 一并注册（评估工作包 5 要求两者） |
| R6 | EOL 硬化 | `baf4209` | .gitattributes 扩至全部文本类型（强制 LF）；build-tokens 读侧 CRLF 规范化；scan-rules F-NEW-1 核实未回退 |
| R7 | 版本三处一致 | `7d5bc5f` | 核验通过（详见 §五）；修复 kit 面上 v5.1 残留三处 |
| R8 | 文档过期标记 | `2a21538` | index.md → "v6.0（治理版骨架）"；design-tokens.md → "由 tokens/*.json 自动生成"；grep "M2 骨架/手工源" 零命中 |
| 部署 | 部署脚本修复与收敛 | `5674916` | deploy/kit/deploy-kit.js 唯一正本（secret 外置/ssh2/bsdtar/资产路径全修复）；恢复 spec-site 加固版；删 kit/deploy 重复资产 |
| R10 | Kit 四入口门禁 | `5adccd4` | check-kit.mjs：四入口 × (渲染/title/资产/控制台/320)；一条命令 32/32 PASS；新门禁首跑抓到 React 320 真溢出并当场修复（.pg-right flex-wrap） |

## 二、统一验证（任务书底部命令，实测输出）

```text
npm run tokens:check       # --check：零差异
npm run tokens:contrast    # PASS 42 / EXEMPT 4 / SKIP 0 / FAIL 0（R17 修正口径；原记录'ALL PASS（46 对）'表述过宽）
npm run rules:check        # 合计注册条款：60（+FND-TOKEN-002）；零漂移
npm run design:check       # 规范站 12 项 PASS + Kit 四入口 20 项 PASS（一条命令）
node verify-colresize.mjs  # F14 FIX: VERIFIED ✅（react + spec-site 双目标）
8095 四入口                # 线上实测 200：/ /react/ /alpine/ /static/ + 核心资产；
                           # check-kit 20/20 PASS（kit-check.json + 8 张截图入 artifacts）
```

## 三、CI 连续红的根因（复验前必读）

e59dcf8 将 M4 期间的三处修复**整体回退**到旧版文件，造成连锁失败：

1. **check.mjs findBrowser 失去 CHROME_PATH 与 Linux 候选** → Actions runner 上找不到浏览器 → design:check 步骤必败（5 连红主因）；
2. **build-tokens.mjs fontFamily 改回恒等映射** → 生成 `sans: [Trebuchet MS, ..., sans-serif]` 裸标识符 = 非法 JS（F17 复发），React 构建必炸；
3. **primitives success-bg 回退 #dcf3f1 但语义层仍 #dff4f2** → 生成器每次运行报 WARN 且与提交产物漂移。

三处均已恢复并对齐（`66d937e`/`41e0957`），CI 恢复绿。**评估工作包 6"success-bg WARN 根因"即第 3 条**：登记值与语义层值不一致，非登记缺失。

另有 e59dcf8 回退 deployed 脚本与 F13（.icon-btn position:relative，随 sync-shared 恢复），已在 §一 前置/部署行记录。

## 四、部署脚本状况（复验会用到）

- 唯一正本 `deploy/kit/deploy-kit.js`：凭据读仓库根 `deploy.secret.json`（不入库）；ssh2 取自 kit/tools/node_modules；win32 用 System32 bsdtar；资产取 deploy/kit/{hub,nginx.conf,Dockerfile}，骨架产物取 kit/skeleton-*；远端 7+1 条资产门禁。
- `deploy/spec-site/deploy.js` 恢复 42c3efc 已 E2E 验证版。
- **8095 已用修复后产物重部署**（部署门禁全过）；8091 规范站内容未变（tokens.css 值对齐后与线上一致），未重部署。
- 事故披露：整改中一次后台试跑意外触发真实部署，执行到远端检查步骤时被终止（此时旧容器已被 `docker rm -f` 移除，8095 短暂下线约 3 分钟），随即以完整部署恢复并复核 200。期间无数据/配置损失。

## 五、R7 核验明细

三处一致**在整改起点即成立**（version.json=5.4.1 ↔ 评审主文档 v5.4 ↔ spec-site title v5.4；design:check VER 项每次运行断言，CI 绿为证）。R7 实际修复的是 kit 面上的 v5.1 残留（content.json 正文、kit/README.md ×2、镜像 label）。**5.4.1 → 6.0.0-rc 的过渡决策按指令原文留待 M6 发布流程**（v6.0 文档已在 R8 中明确标注"治理版进行中、真源仍为 5.4.1"）。

## 六、R10 门禁语义（复验口径）

- `npm run design:check`（或 CI 中 env：CHECK_URL + KIT_URL）= tokens:check + 规范站 12 项 + Kit 四入口 20 项，任一失败整体红；
- Kit 每入口断言：非空白渲染（React 以 #root 子节点数判空）、title 非空、全部子资源 200（含 requestfailed，即"网络清单为空"）、控制台零错误、320px 根级无溢出——与评估工作包 2 的完成定义逐项对应；
- 产物：`kit/tools/design-check/artifacts/`（design-check.json、kit-check.json、规范站基线 ×2、四入口截图 ×8）。

## 七、环境变化提示（移交方/评估方注意）

- 站点地址默认值已改为 **10.20.30.203**（10.10.10.218 在 VPN 环境不可达）；CI/脚本均可用 CHECK_URL / KIT_URL 环境变量覆盖；
- 本地跑 `npm run design:check` 需先起 spec-site 服务并设 CHECK_URL，或直接依赖 CI；
- 服务器密码维持旧值（负责人裁决），`deploy.secret.json` 与 D 盘 txt 已同步。

## 八、复核申请

- **新固定 SHA：`7596e2c`**（R10 提交 `5adccd4` 之后唯一代码提交：alpine favicon 声明 + runner pip 回退；其后仅 docs 报告）。
- 请按《评估意见合并与整改指令》§四复验：四入口 × 双断点 + 网络清单为空 + 门禁绿（CI 台账见 .github/ci-state.json 与 git log 的 ci: 提交）。
- 复核通过后：M4 关闭 → 恢复 **M5 三项目试点**（TSSKB / dan-cpl / nvci，各仓库 v6-pilot 分支——整改期间按指令已暂缓）。

## 九、运行台账补记（推送后补充）

1. R10 首跑（run 34756655926，sha `5adccd4`）failure：本地按 runner 流程完整复现，定位两处——
   ① ALPINE 入口未声明 icon，浏览器自动请求 /favicon.ico；nginx 容器 try_files 回退把它掩盖为 200（线上绿），纯 http.server（CI 装配目录）下真 404 → 根修：alpine/index.html 补 `<link rel="icon">`（`7596e2c`）；
   ② runner 系统 pip 受 PEP 668 限制，jinja2 安装加三级回退（`7596e2c`）。
   修复后本地全流程复跑 design:check 32/32 PASS。
2. run 34757608069（sha `7596e2c`）被并发组取消（报告提交 4cb3ad6 紧随其后触发新 run），其 cancelled 结论已入台账（506b659）。
3. 本节提交触发新一轮完整门禁，结果以最新 .github/ci-state.json 为准。
