# 评估委托书（给第三方评估 AI）

> 发出：项目负责人（经移交方整理）。你是本项目的**独立评估方**，与建设方（原移交方 AI）、接手方（现开发 AI）互不隶属。
> 评估对象已固定：**commit `3312bf0`**（若需评估更新的提交，由负责人另行指定并说明差异范围）。

## 你的角色约束（必须遵守）

1. **只评估，不修改**：不得向 main 推送任何提交；如需留下书面意见，写入你本地的工作副本或以文本形式返回给负责人。
2. **不索取部署权限**：不需要、也不应获得服务器 SSH 密码。站点验证一律通过 **HTTP 只读访问**（下文 URL）。
3. **先盲评、后对照**：先独立形成自己的结论并写下来，然后再阅读 `docs/review/` 中建设方与接手方的评审/验收报告做对照，指出"我发现了而他们没写"与"他们写了而我怀疑"的部分。
4. **对产物说话**：每条评估意见必须挂钩可验证的实物（文件路径 / 命令输出 / URL / 截图），不接受"感觉良好"式结论。

## 评估材料（全部在仓库与两个在线站点内）

| 材料 | 位置 |
|---|---|
| 项目背景与完整交接 | `AGENTS.md` → `docs/HANDOVER.md` |
| 规范本体（四层） | `docs/v6/`（00-foundations / 10-patterns / 20-components / 30-governance + index + USAGE） |
| 历次评审与验收证据 | `docs/review/`（专家两轮、M4 收尾报告、M5-0 前置报告、移交方两份复核） |
| 令牌真源与生成链 | `kit/tokens/*.json` + `kit/tools/build-tokens.mjs` |
| 三骨架与演示站源码 | `kit/skeleton-*` + `spec-site/` |
| 门禁 | `kit/tools/`（design:check / tokens:contrast / rules:check） |

## 在线站点（两地址为同一服务器，按你的网络可达性选用）

- http://10.20.30.203:8091 （规范演示站）· http://10.20.30.203:8095 （Kit 三骨架，/react/ /alpine/ /static/）
- http://10.10.10.218:8091 · http://10.10.10.218:8095 （备选地址；部分网络只通其中一个）
- 注意 :8096 为接手方的临时页，其用途已要求说明，可作为提问项。

## 建议的评估动作（按需选用，全部只读或本地）

1. **文档审读**：HANDOVER → docs/v6 四层 → 两份收尾报告的声明，对照 `kit/` 实物逐条核验；
2. **本地跑门禁**（需 Node + Edge/Chrome）：`cd kit/tools && npm install && npm run design:check --url <站点URL>`、`npm run tokens:contrast`、`npm run rules:check`；
3. **在线站点实测**：浏览器打开 8091/8095，操作 14A 表格（排序/列宽拖动+键盘/分页/搜索/批量/亮暗主题/中英切换），320px 视口看溢出，用 `document.fonts.check('16px "Noto Sans SC"','中')` 验证字体真实加载；
4. **规则 ID 抽查**：从各文件注册表抽 ID 反查正文条款是否真实存在；
5. **Git 考古**：`git log`/`git show` 核对报告中的提交号与变更是否一致。

## 已知限制（评估时请如实计入）

- 仓库为 Private；Git 历史中存在旧服务器口令残留（4 个历史提交，决策 A 已接受，接手方已做密码外置 + 轮换）——评估安全状况时区分"当前树"与"历史"；
- 接手方环境与你的环境必有差异（AGENTS.md 有双机环境差异表），门禁在 LF 检出下应全绿；若你在 autocrlf=true 环境遇到 rules:check 假 DRIFT，请确认其修复（F-NEW-1，3df753d）是否覆盖你的场景并如实报告；
- 两轮外部专家评审（78 → 80 分）原件在 `docs/review/`，其改进建议的落实状态是本次评估的合理范围。

## 输出要求

返回给负责人一份报告：① 总评分与分维度评分；② 每条意见挂验证实物；③ "必须修 / 建议改 / 可接受"三级分类；④ 对 docs/v6 验收清单的逐项判定。负责人会将你的报告合并进既有意见流，转接手方执行。
