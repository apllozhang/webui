# ALE WebUI v6.0 M4 收尾合并整改指令

> 发出：移交方（合并外部评估意见后）。执行：接手方 AI。
> 前提：M4 收尾已验收通过，M5-0 前置已完成（密码恢复旧值、凭据外置、F-NEW-1 EOL 修复）。
> 目标：修完本页全部条目后，接手方以新固定 SHA 申请复核，通过即关闭 M4 进入 M5 试点。

---

## 逐条整改（全部必须，不可选择性跳过）

| # | 问题 | 修法 | 完成判定 |
|---|---|---|---|
| R1 | React `vite build` 缺 `--base=./` → 子路径部署资产 404 | `vite.config.ts` 固化 `base:"./"`；`package.json` build 脚本同步 | `/react/` 在 8095 下 CSS/JS/图片全部 200 且页面渲染正确 |
| R2 | Alpine 320px 表格逐字竖排 | `table.data` 外包 `overflow-x:auto` 容器（已有）；给 `table.data` 设 `min-width:640px` 使窄视口触发容器滚动而非压缩列 | 320px 下表头文字完整、数据不逐字断行 |
| R3 | Static `base.html` 页脚绝对路径 Logo | 全文件搜索 `src="/assets/` → 改 `src="assets/` | `/static/` 页脚 Logo 200 |
| R4 | Static 首页 title 为空 | `content.json` home 节补 `"title": "首页"`；`build.py` 渲染时传入 | `/static/` title 非空且含站名 |
| R5 | `rules:check` 未在 package.json 注册 | `kit/tools/package.json` 加 `"rules:check": "node scan-rules.mjs --check"` | `npm run rules:check` 退出码 0 |
| R6 | `rules:check` 在 autocrlf=true 检出报 23 DRIFT | `scan-rules.mjs` 读文件后 `replace(/\r\n/g,"\n")`；`.gitattributes` 对生成物强制 `eol=lf` | Windows CRLF 检出 `rules:check` 零漂移 |
| R7 | version.json 5.4.1 ↔ 文档 v5.4 ↔ 站点 v5.4 统一 | version.json `version` 改 `"5.4.1"`；文档标记 v5.4；HTML title 同步；site title 同步 | 三处一致 |
| R8 | 文档过期标记：design-tokens.md 仍写"当前为手工源" / index.md 仍写"M2 骨架" | design-tokens.md 改"由 tokens/*.json 自动生成（build-tokens.mjs）"；index.md 标题改"v6.0（治理版骨架）" | 无"M2/手工源"等过时表述 |
| R9 | Alpine 320 表格列宽压缩→逐字竖排 | Alpine 表格（skeleton-alpine/index.html）设 `min-width:640px` + 外容器 `overflow-x:auto`；窄视口横向滚动而非压缩 | 320px 表格数据不逐字断行 |
| R10 | design:check 扩展：遍历 Kit 四入口 | check.mjs 或新脚本覆盖 `/react/` `/alpine/` `/static/`；空白页/破图/空 title/控制台错误必拦 | 一条命令覆盖 8091 规范站 + 8095 四入口 |

## 完成后统一验证

```bash
cd kit/tools
npm run tokens:check       # 零差异
npm run tokens:contrast    # 46 对 ALL PASS
npm run rules:check        # 零漂移
npm run design:check       # 12 项 PASS
node verify-colresize.mjs  # 列宽独立性 PASS
# 8095 四入口人工/脚本验证各 200 + 渲染正确
```

## 修复顺序建议

R1（React 资产）→ R3+R4（Static）→ R2+R9（Alpine 320）→ R5–R8（工具/治理）→ R10（门禁扩展）→ 统一验证 → 新 SHA 复核。

## 明确不做（本轮）

- 不改视觉风格
- 不新增组件
- 不拆规范文档（留 M5 后）
- 不动三骨架已有核心逻辑（只修部署/样式/注册层面）
