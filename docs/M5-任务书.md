# M5 任务书（移交方 → 接手方，2026-09-12）

> 前置状态：M4 收尾已验收通过（见 `review/2026-09-12-M4收尾复核报告-移交方.md`，6/6）。
> 本任务书覆盖 **M5 前置必做 + M5 三项目试点**。按顺序执行，每完成一项推一次证据（沿用既有节奏）。

## M5-0 前置必做（阻塞试点开工）

| # | 任务 | 验收标准 |
|---|---|---|
| 0.1 | **F-NEW-1 修复**：scan-rules.mjs 读文件补 EOL 规范化（`replace(/\r\n/g,"\n")`），写回统一 LF；仓库加 `.gitattributes`（`* text=auto` + `*.md eol=lf`）；教训入 lessons.md（F16） | 在 autocrlf=true 的 Windows 检出上 `rules:check` 不再报 DRIFT；CI 仍绿 |
| 0.2 | **密码外置改造**：deploy.js / deploy-kit.js 改读未入库的 `deploy.secret.json`（提供 `deploy.secret.example.json` 模板 + README 说明字段）；脚本启动检测缺失即提示并退出，不再含任何硬编码口令 | 仓库全文 grep 无口令；本机带 secret 文件部署仍可跑通 |
| 0.3 | **服务器密码轮换**（需移交方配合：确认新密码或授权执行 `passwd`）：轮换后同步两处——`deploy.secret.json` 与 `D:\AIWork\10.20.30.203.txt` | 新密码部署一次成功（门禁全绿）；旧密码失效 |

> 顺序建议：0.2 先做（代码侧，独立可验）→ 0.3（轮换后立即写入 secret 文件验证）→ 0.1 随时可做。

## M5 三项目试点（v6.0 发布硬条件）

**总约束**：每项目只选 1–2 个代表页；试点改造在**各自仓库的 `v6-pilot` 分支**进行，不动 main、不动线上；不改业务 API/逻辑；记录迁移耗时、缺失令牌、组件例外、视觉差异。

| 项目 | 试点页（建议，负责人可改） | 用哪条链路 | 重点验证 |
|---|---|---|---|
| TSSKB | 任一课程内容页 | Static + content-site 模式 | 阅读宽度 `--reading-max`、内容层级、静态资源 200 |
| ale-dan-cpl-system | 报价列表页 | React + 工作台外壳 + DataTable Core/Advanced | App Shell 嵌入、14A 对齐、批量操作 |
| nvci-lite | 向导第 1→2 步 | Alpine + wizard-flow 模式 | Stepper、跨步数据保持、轻量档升级边界判定 |

**每试点的交付物**（入 `docs/v6/30-governance/` 或试点分支 README）：
1. 改造前后截图（1440 + 320）
2. 迁移耗时与"缺失令牌/组件"清单
3. 视觉差异记录与原因
4. 全部偏离登记进 `design-exceptions.yml`（**首次真实启用**——无复审日期的例外不接受）

## M5 验收标准（移交方将按此复核）

- 三试点均使用同一套语义令牌；无项目专属值污染 primitive；
- `design:check` / `tokens:contrast` / `tokens:check` / `rules:check` 全绿；
- design-exceptions.yml 存在真实条目且字段完整（规则 ID / 负责人 / 复审日期）；
- 试点报告齐备，且三个来源项目的维护者（或负责人）各确认一次"没有被迫长成同一个页面，但看起来是同一家"。

## 明确不做

- 不全量重构三个项目；不改业务 API；不引入新组件库；不动线上容器（8091/8095 两站除外——它们本身就是本项目产物）。

## 完成后

汇报证据（提交号 + 运行 URL + 截图），移交方验收通过即进入 **M6**：干净交付包（make-release）+ v6.0 tag + Release Notes + 三方签字。
