# M5.2 第三轮合并指令(R20 + M5 试点正式启动)

> 依据:第三轮独立评估(实际核验 `2b2af564`,91/100,**PASS——适用于 M5 受控试点**;V6.0 正式发布仍未放行)+ 移交方独立复核。
> 执行:接手方 AI。锚定 `2b2af564`(与 fcffd3b 仅差 CI 台账提交,代码同源)。
> 归档说明:第三轮报告原件在评估方环境(F 盘路径),本机不可达;本指令以项目负责人转述的结论 + 移交方复核为准,报告原件取得后归档至 `docs/review/2026-09-14-第三轮独立评估报告-2b2af564.md`。

## 〇、移交方复核声明

1. **接受 91/100 PASS 裁决**(适用域:M5 受控试点)。三轮轨迹:68 BLOCKED → 82 CONDITIONAL → 91 PASS,整改闭环得到独立第三方确认。
2. **第三轮唯一新发现 R2-P2(同意复选框错误态缺 `aria-invalid=true`):移交方代码 + 线上双重复核属实**——`Controls.tsx` Checkbox 仅有 `ariaRequired`/`describedBy` 通道,无 invalid 通道;线上实测错误态 `#demo-agree` 的 `aria-invalid=null`(而 `aria-required=true`、`aria-describedby=demo-agree-error` 均在)。列为 **R20**。
3. 评估方其余判断与本方验收记录一致:37/37 门禁、R11 关闭、注入回归门禁变红、R19"里程碑内未到期"(三仓库均无 v6-pilot 分支,与仓库状态相符)。

## 一、R20(P2,小改,随 M5 启动批一并完成)

- 修复:`Checkbox` 组件增加 `invalid?: boolean` 通道 → 错误态渲染 `aria-invalid=true`(非错误态不渲染);`FormsDemo.tsx` 将 `agreeError` 状态传入。
- 门禁固化:R18 的 `REACT-IX-AGREE` 断言扩展——未勾选提交后 `#demo-agree` 的 `aria-invalid` 必须为 `"true"`。
- DoD:线上实测错误态 `aria-invalid=true`;37 项门禁全绿(断言扩展后仍 37 项,IX-AGREE 增强而非新增条目)。

## 二、M5 三项目试点正式启动(评估方 PASS + 移交方验收双重放行)

1. **试点分支**:TSSKB / dan-cpl / nvci 各仓库建 `v6-pilot` 隔离分支,各 1–2 页真实页面,引用本 kit 构建(不动各仓库 main)。
2. **启动提交必须包含**:
   - **R19 落地**:`design-exceptions.yml`(空数组合法起步)+ CI 校验(schema / owner / review-by;缺 owner、缺复审日期、过期、未知规则 ID 均 FAIL)+ R17 的 4 项 EXEMPT 对比度记录作为首批登记(含规则出处"规范 15 章附录 G"与 owner);
   - **R20 修复**(§一);
   - 试点页代码与构建脚本。
3. **试点页要求**:遵循 v6 规范与门禁口径(设计令牌引用、对比度、键盘可达、320/1440 双断点);每页跑 `design:check` 同款断言口径(可按页面裁剪,但交互断言不得省略)。
4. **M5 首轮报告**(docs/review/):三仓库分支与页面清单、页面地址/截图、R19 台账与 CI 校验证据、R20 修复证据、试点中发现的规范缺口清单(回流到 v6 文档)。

## 三、M6 冻结项重申(第三轮再次确认为发布阻断项)

- **版本真源一次性切换**(5.4.1 → 6.0.0-rc):version.json、站点 title、README、包版本、design-tokens.md frontmatter 等全部"M2 迁移/进行中"标记清零;
- **字体真实分片与传输预算**(unicode-range + 首屏预算门禁);
- 三方签收(建设方/验收方/评估方)→ v6.0 Release。
在上述完成前,任何场合不得表述"V6.0 已发布/已就绪"。

## 四、验证命令(推送前自测)

```powershell
$env:CHECK_URL='<你的可达视图>'
$env:KIT_URL='<同上>'
cd kit/tools
npm run tokens:check        # 零差异
npm run tokens:contrast     # PASS 42 / EXEMPT 4 / SKIP 0
npm run rules:check         # 零漂移
npm run design:check        # 37/37(IX-AGREE 含 aria-invalid 断言)
node verify-colresize.mjs   # 双目标 PASS
```
